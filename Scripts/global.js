import "./Modules/icon-loader.mjs";
import "./Modules/header.mjs";
/* Should be in every site, handles stuff like the header or loading of icons */

/* ICONS */
/*
    Loads icons into <svg> tags when it sees that the tag has dataset.icon (in CSS terms: svg[data-icon="something"])
    It gets these icons from the /Icons directory. If it cannot find the icon, it leaves the svg untouched.
*/

/* HEADER */
/*
    Literally just loads the header.
    That's all it does.
*/